awk 'BEGIN{split("water",w,"")for(i in w)c[w[i]]=i-1 n=0 for(i=1;i<=length(ENVIRON["WATER"]);i++)n=n*5+c[substr(ENVIRON["WATER"],i,1)] split("stir.",s,"")for(i in s)c[s[i]]=i-1 m=0 for(i=1 
i<=length(ENVIRON["STIR"]);i++)m=m*5+c[substr(ENVIRON["STIR"],i,1)] t=n+m split("bestchol",o,"")r="" while(t>0){r=o[(t%9)+1] r t=int(t/9)} print r}'
